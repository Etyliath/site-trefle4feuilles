<nav class="navbar navbar-expand-lg ">
    <div class="container-fluid">
        <a class="navbar-brand ms-lg-3" href="{{ path('admin.dashboard.index') }}">
            <img src="{{ asset('images/logo.png') }}" alt="" height="40">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {{ app.current_route == "admin.dashboard.index" ? 'active' : '' }}"
                       href="{{ path('admin.dashboard.index') }}">Dashboard</a>
                </li>
                <li class="nav-item dropdown me-5">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                       aria-expanded="false">
                        Administration
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('admin.creations.index') }}">Créations</a></li>
                        <li><a class="dropdown-item" href="{{ path('admin.categories.index') }}">Catégories</a></li>
                        <li><a class="dropdown-item" href="{{ path('admin.comments.index') }}">Commentaire</a></li>
                        <li><a class="dropdown-item" href="{{ path('admin.orders.index') }}">Commandes</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="{{ path('admin.profile.users') }}">Gestion profil</a></li>
                    </ul>
                </li>
                <li class="nav-item ms-md-3">
                    <a class="nav-link" href="{{ path('home') }}">Site</a>
                </li>
                <li class="nav-item dropdown me-5">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                       aria-expanded="false">
                        {{ app.user.username }}
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('profile.edit') }}">Profil</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a></li>
                    </ul>
                </li>

            </ul>
        </div>
    </div>
</nav>