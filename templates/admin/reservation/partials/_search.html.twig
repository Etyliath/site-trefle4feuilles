<div class="form-group mb-3">
    <form action="{{ path( 'admin.reservations.index' ,
        {filterOrder: 'filterReservation',filterUsernameOrEmail:'filterUsernameOrEmail',filterStatus : 'filterStatus'} ) }}"
          method="get"
          class="d-flex justify-content-center">
        <div class="d-flex px-2 bg-white rounded-5 ">
            <input type="text" id="filterReservation" name="filterReservation" value="" class="form-control border-0 me-1"
                   placeholder="N° réservation">
            <input type="text" id="filterUsernameOrEmail" name="filterUsernameOrEmail" value=""
                   class="form-control border-0" placeholder="Pseudo">
            <select name="filterStatus" id="filterStatus" class="form-select border-0 ">
                <option value="all" {{ filterStatus == null ? 'selected' : '' }}>Tous</option>
                <option value="pending" {{ filterStatus == "pending" ? 'selected' : '' }}>En attente</option>
                <option value="reserved" {{ filterStatus == "reserved" ? 'selected' : '' }}>Réserver</option>
                <option value="delivered" {{ filterStatus == "delivered" ? 'selected' : '' }}>Livrer</option>
            </select>
            <button class="btn btn-light mx-1" type="submit"><i class="bi bi-search"></i></button>
        </div>
    </form>
</div>