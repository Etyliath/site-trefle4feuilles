{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
    <section class="mt-5 d-flex flex-column justify-content-center align-items-center">
        <h5>Votre paiement a été annulé</h5>
        <div>
            <p class="fst-italic">le site du trèfle à 4 feuilles</p>
            <div class="d-flex justify-content-center align-items-center gap-2">
                <a href="{{ path('home') }}" class="btn btn-secondary">Retour à l'accueil</a>
                <a href="{{ path('orders.user') }}" class="btn btn-secondary">Retour aux commandes</a>
            </div>
        </div>
    </section>
{% endblock %}